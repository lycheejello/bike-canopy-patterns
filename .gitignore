# node
node_modules/
npm-debug.log*

# os / editor
.DS_Store
*.swp

# emulator scratch / local device backups (commit deliberately, not in bulk)
backup/
*.local.json

# python venvs (push-patterns.py tooling) — use Apple python / a --symlinks venv
.venv*/
__pycache__/
